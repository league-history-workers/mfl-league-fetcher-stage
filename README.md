# MFL League Fetcher

Dedicated worker repository for historical MFL league ingestion.

The worker produces immutable chunks for the existing append-only cache. Canonical schema validation and serialized index merging remain unchanged.
